quarkus:
  http:
    port: 24188

  log:
    category:
      #"org.apache.shiro":
      #  level: DEBUG
      "com.dropchop":
        level: DEBUG

  compose:
    devservices:
      files: src/main/docker/compose-devservices-test.yaml

  hibernate-orm:
    #log:
    #  sql: true
    #  bind-parameters: true
    database:
      generation: update
